{
	"name": "build",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "node-gyp rebuild && node -e \"require('addon-tools-raub/cpbin')('opencl')\""
	},
	"dependencies": {
		"addon-tools-raub": "5.0.0"
	}
}
